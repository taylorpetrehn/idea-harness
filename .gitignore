# Dependencies
node_modules/

# Editor + OS
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env / secrets
.env
.env.local
.env.*.local

# Claude Code session-local artifacts (don't commit these), but DO commit
# the tracked harness primitives at .claude/{CLAUDE.md,settings.json,hooks/,agents/}.
.claude/.ralph-state*
.claude/.evidence-reads
.claude/scheduled_tasks.lock
.claude/settings.local.json

# Local v2-alpha CLI runtime artifacts (preserved on disk, not tracked).
# The CLI is archived at tag archive/v2-alpha; these dirs are effectively dead.
ideas/
runs/
state/
context/

# Python (Phase 2 harness-cli)
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/
