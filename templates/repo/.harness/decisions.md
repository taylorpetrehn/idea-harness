# {PROJECT} — Conscious Decisions

> Append-only ledger of deliberate non-choices for {PROJECT}. Each entry
> is one decision with the reason. The idea-harness brainstormer cites
> these to avoid re-litigating settled questions; PR-time agents check
> this file when introducing new top-level features.
>
> Format: `### YYYY-MM-DD — <one-line decision>` then 1–3 sentences of why.
>
> The harness reads this file when projects.yml has
> `decisions_path: ".harness/decisions.md"` for this project. See
> `~/.claude/skills/idea-harness/SKILL.md` "Per-project context resolution".

---

### YYYY-MM-DD — STUB
Replace this placeholder with the first real decision before onboarding
this project to the harness. Until at least one real entry exists here,
the brainstormer treats the project as having no settled non-choices.
