# Skill — moved

The operational SKILL.md for the idea-harness pipeline now lives at:

```
~/.claude/skills/idea-harness/SKILL.md
```

This file is left as a redirect so that any tooling or memory still pointing
here gets a clear pointer to the new canonical location. The previous content
of this file (describing the v2-alpha CLI's six-layer architecture) is preserved
in the git history at tag `archive/v2-alpha`.

See [README.md](./README.md) for the new harness model — Mac launch workspace +
three local LaunchAgents + a vanilla Claude Code skill.
